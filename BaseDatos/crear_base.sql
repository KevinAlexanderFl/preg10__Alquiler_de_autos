CREATE DATABASE IF NOT EXISTS alquiler_autos
CHARACTER SET utf8mb4
COLLATE utf8mb4_spanish_ci;

-- Las tablas, relaciones y datos iniciales se crean automáticamente
-- mediante la migración incluida al ejecutar: dotnet run
